# Prompt — RunPod SSH Bootstrap

```text
Read .codex/goals/00-runpod-ssh-bootstrap.md and codex/RUNPOD_SSH_DEPLOYMENT.md. Inspect infra/runpod files. Create or update only the files needed to verify SSH/GPU/Docker/direct-process readiness. Run dry-run checks locally. Do not print secrets. End with a RunPod readiness report.
```
